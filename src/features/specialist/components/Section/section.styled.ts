import { Section } from '@features/ui';
import { GRAY_SCALE } from '@styles';
import styled from 'styled-components';

const EspecialistSectionBackground = styled(Section)`
  background: ${GRAY_SCALE[1000]};
  position: relative;
`;

export {
  EspecialistSectionBackground
};
